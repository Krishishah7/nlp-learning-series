# Hybrid Routing in RAG

## Overview
This project improves query routing in a RAG system by combining classification with retrieval confidence to make smarter routing decisions.

## Features
- Query classification
- Confidence-based validation
- Fallback handling
- More robust multi-domain RAG

## How It Works
Query → Classify → Retrieve → Confidence Check → Answer / Fallback
